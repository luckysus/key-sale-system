# 贡献指南

感谢贡献。提交改动前请先确认它适用于通用部署，而不是某个实例的私有配置。

## 开发流程

1. 从最新分支创建功能分支。
2. 使用现有的 Python、Node.js 和测试工具完成改动。
3. 运行 `npm test` 和 `npm run build`。
4. 提交信息简短描述行为变化，并在 PR 中说明测试结果。

## 公开仓库规则

- 不提交 `.env`、数据库、日志、构建产物、备份归档、证书、SSH 私钥或任何账号凭据。
- 域名、IP、绝对路径和邮件地址使用文档域名或环境变量表示。
- 新增配置时同步更新 `deploy/*.env.example` 和 README。
- 安全漏洞请按 `SECURITY.md` 的私密渠道报告，不要在公开 issue 中粘贴敏感数据。
